.DS_Store
node_modules
.turbo
*.log
.next
dist
dist-ssr
*.local
.env
.cache
server/dist
public/dist
out/
coverage/

# playwright-cli session artifacts
.playwright-cli/

# Local engineering documentation (design system, ADRs, audits, screenshots).
# Excluded from git per review — kept only in the working copy.
engineering/
