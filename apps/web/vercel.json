{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  // "buildCommand": "cd ../.. && yarn install && yarn turbo run build --filter=prism-web...",
  "outputDirectory": "dist",
  "framework": "vite"
}
