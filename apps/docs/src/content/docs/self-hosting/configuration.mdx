---
title: Configuration reference
description: Every environment variable, validated from the runtime config.
audience: Operator
scope: Self-hosted
lastReviewed: "2026-08-11"
---

import SectionLabel from '../../../components/docs/SectionLabel.astro';
import ConfigTable from '../../../components/docs/ConfigTable.astro';


<SectionLabel label="ENVIRONMENT CONTRACT" />

Every variable below is validated at boot by `resolvePrismConfig`: invalid
values fail fast with the variable name and remediation (never values).
This table is generated from the validator, `.env.example`, and
`.dev.vars.example` — keep those three in sync.

## Required

<ConfigTable
	caption="Required variables — validated at boot"
	rows={[
		{ name: 'DATABASE_URL', requirement: 'PostgreSQL connection URL (plain or Neon-style pooled)', default: '—', mode: 'both', secret: true, restart: 'boot' },
		{ name: 'JWT_SECRET_KEY', requirement: 'Minimum 32 characters — `openssl rand -hex 32`', default: '—', mode: 'both', secret: true, restart: 'boot' },
		{ name: 'CLIENT_URL', requirement: 'Dashboard origin (CORS allowlist, email links); origin-shaped URL', default: '—', mode: 'both', restart: 'boot' },
		{ name: 'BASE_URL', requirement: 'Public API URL (cookies, callbacks, JWKS); origin-shaped URL', default: '—', mode: 'both', restart: 'boot' },
		{ name: 'SETUP_TOKEN', requirement: 'Required in self-hosted; minimum 16 characters', default: '—', mode: 'self-hosted', secret: true, restart: 'boot' },
		{ name: 'PORT', requirement: 'Node server port', default: '8787', mode: 'self-hosted', restart: 'boot' },
	]}
/>

`BASE_URL` behind a reverse proxy must be the **external** origin users
reach (e.g. `https://analytics.example.com`), not an internal service name.

## Deployment mode

| Variable | Values | Default | Notes |
| --- | --- | --- | --- |
| `PRISM_DEPLOYMENT_MODE` | `hosted` \| `self-hosted` | `hosted` | Strict: anything else fails boot |
| `ENVIRONMENT` | `development` \| `production` | `development` | `production` enables Secure cookies; strict validation |
| `SIGNUP_POLICY` | `open` \| `invite-only` \| `disabled` | `disabled` (self-hosted) / `open` (hosted) | `invite-only` and `disabled` close public signup; invites + first-owner bootstrap remain |
| `INSTANCE_NAME` | Display name in auth shell, emails, config endpoint | `Prism` | Never hardcoded `Prism Cloud` |

`ALLOW_PUBLIC_SIGNUP` is the legacy alias for `SIGNUP_POLICY` (hosted
compatibility).

## Mail

| Variable | Requirement | Default |
| --- | --- | --- |
| `MAIL_SMTP_HOST` | With `MAIL_SMTP_PORT` etc., enables SMTP (takes precedence) | — |
| `MAIL_SMTP_PORT` | e.g. `587` (STARTTLS) or `465` (implicit TLS) | — |
| `MAIL_SMTP_SECURE` | `true` for implicit TLS | `false` |
| `MAIL_SMTP_USER` / `MAIL_SMTP_PASS` | Auth for the SMTP server | — |
| `MAIL_FROM` | Sender, e.g. `Prism <no-reply@example.com>` | — |
| `RESEND_API_KEY` | Used when SMTP is not configured | — |

No mail channel → verification/reset links print to the log only.

## Social providers

| Variable | Requirement |
| --- | --- |
| `GITHUB_CLIENT_ID` + `GITHUB_CLIENT_SECRET` | Both required to enable GitHub sign-in |
| `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` | Both required to enable Google sign-in |

Callbacks: `{BASE_URL}/api/auth/callback/{github|google}`.

## Analytics store

| Variable | Requirement | Default |
| --- | --- | --- |
| `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` | Optional | — |
| `ANALYTICS_RETENTION_DAYS` | Non-negative integer; unset/0 disables retention deletion | `0` (disabled) |

Without the store, analytics reads return empty and the rest of the instance
works (one-time warning at boot). Self-hosted defaults to the bundled sqld
container; set these only when using Turso.

`ANALYTICS_RETENTION_DAYS` enables batched deletion of expired sessions and
events (events first, in one atomic batch). Unset or `0` disables deletion.
Run `yarn workspace prism-analytics-api retention --status|--dry-run` to
inspect before applying (see the self-hosting docs).

## Object storage

<ConfigTable
	caption="Object storage variables"
	rows={[
		{ name: 'STORAGE_DRIVER', requirement: '`local` | `s3` | `imagekit`', default: 'local (self-hosted) / imagekit (hosted)', mode: 'both', restart: 'boot' },
		{ name: 'STORAGE_LOCAL_DIR', requirement: 'Local files directory (mounted volume)', default: './data/uploads', mode: 'self-hosted', restart: 'boot' },
		{ name: 'STORAGE_S3_ENDPOINT', requirement: 'Required when `s3`', default: '—', mode: 'self-hosted', secret: true, restart: 'boot' },
		{ name: 'STORAGE_S3_REGION', requirement: 'Required when `s3`', default: '—', mode: 'self-hosted', restart: 'boot' },
		{ name: 'STORAGE_S3_BUCKET', requirement: 'Required when `s3`', default: '—', mode: 'self-hosted', restart: 'boot' },
		{ name: 'STORAGE_S3_ACCESS_KEY_ID', requirement: 'Required when `s3`', default: '—', mode: 'self-hosted', secret: true, restart: 'boot' },
		{ name: 'STORAGE_S3_SECRET_ACCESS_KEY', requirement: 'Required when `s3`', default: '—', mode: 'self-hosted', secret: true, restart: 'boot' },
		{ name: 'STORAGE_PUBLIC_URL', requirement: 'Public object base URL; URL-validated, may include bucket prefix path', default: '—', mode: 'self-hosted', restart: 'boot' },
		{ name: 'IMAGE_KIT_API_KEY', requirement: 'Required when `imagekit`', default: '—', mode: 'both', secret: true, restart: 'boot' },
	]}
/>

Hosted deployments reject `s3`/`local` (Worker has no filesystem). See
[Storage](/self-hosting/storage/).

## Networking & integrations

| Variable | Purpose |
| --- | --- |
| `CORS_ALLOWED_ORIGINS` | Extra dashboard origins, comma-separated; **exact origins only** (wildcards rejected, including `*.`) |
| `IP_INFO_API_TOKEN` | Optional IP geo enrichment (non-blocking) |

## Secrets & rotation

- Generate: `openssl rand -hex 32` (`JWT_SECRET_KEY`), `openssl rand -hex 24`
  (`SETUP_TOKEN`), and a strong `POSTGRES_PASSWORD`.
- Rotating `JWT_SECRET_KEY` invalidates all sessions — rotate during a
  maintenance window.
- `SETUP_TOKEN` is only read at first boot; it can be removed from the
  environment after the owner exists (the endpoint closes permanently).
