// @prism/react — the thin provider/hook layer over @prism/core.
//
// The v1 client bindings were removed in task-9 slice 6; the v2 provider
// and hook land in slice 8 (React compatibility) after the browser
// adapter (slice 7).
export {};
